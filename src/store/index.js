import { createStore } from 'vuex'
import {juegos } from '../db/juegos.json'
export default createStore({
  state: {
    juegos
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
